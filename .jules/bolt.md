# Bolt's Journal

## 2024-05-22 - [Initial Setup]
**Learning:** The project structure in `README.md` is misleading. It describes a Rust project, but the codebase is Python (`orionx`).
**Action:** Trust the file system (`list_files`) over the documentation when they conflict. Focus on `orionx` directory for optimizations.
